window.MCCDocContent = {
  "CLAUDE.md": [
    "# CLAUDE.md — Local Business Manager (LBM) v1",
    "",
    "This file is loaded automatically when this folder is open as a workspace in Claude Code. Read it before responding to any request.",
    "",
    "---",
    "",
    "## WHAT THIS IS",
    "",
    "The **Local Business Manager (LBM)** is a self-contained, browser-based task tracker. It is designed to be a standalone project — open this folder directly in Claude Code and it works independently.",
    "",
    "This is **v1** — a self-contained snapshot with sample tasks and full documentation included.",
    "",
    "---",
    "",
    "## FILE MAP",
    "",
    "```",
    "Local Business Manager/",
    "├── CLAUDE.md              ← you are here (session bootstrap)",
    "├── SKILL.md               ← how to develop and extend the LBM",
    "├── PHASES.md              ← phase handover guide with staged prompts",
    "├── README.md              ← setup + usage guide",
    "├── index.html             ← main app (List + Board views)",
    "├── docs.html              ← documentation viewer",
    "├── resources.html         ← resource index (logos, assets)",
    "├── styles.css             ← all styles for every page",
    "├── task-app.js            ← list + board + detail panel logic",
    "├── docs-app.js            ← docs viewer logic",
    "├── docs-content.js        ← pre-rendered doc content cache",
    "├── project-data.js        ← seed tasks, docs index, areas config",
    "├── docs/                  ← LBM documentation",
    "│   ├── LOCAL_PROJECT_SYSTEM.md    ← tracker workflow guide",
    "│   └── PERSISTENCE_AND_STATE.md  ← data storage reference",
    "└── resources/             ← design assets",
    "    └── README.md          ← how to add/manage resources",
    "```",
    "",
    "---",
    "",
    "## KEY DESIGN RULES",
    "",
    "- **Vanilla JS + HTML/CSS only** — no build step, no npm. Open `index.html` in a browser.",
    "- **Local-first** — all task state lives in `localStorage`. The seed data in `project-data.js` is the git-tracked baseline.",
    "- **Notes field** — `task.notes` is plain text (preview). `task.body` is HTML (rich text from the detail panel editor).",
    "- **Lane system** — `processing` and `on-hold` are separate lane values but display under the same \"Processing / On Hold\" board column.",
    "- **Board collapse** — collapsed columns are stored in `localStorage` under `ui.collapsedColumns`. They render as vertical strips on the right side of the board.",
    "- **Resources** — files in `resources/` are tracked in git but excluded from any app build process.",
    "",
    "---",
    "",
    "## ACTIVE LANES",
    "",
    "| Lane key | Display label | Active? |",
    "|---|---|---|",
    "| `newly-added-or-updated` | Newly Added or Updated | ✓ |",
    "| `backlog` | Backlog | ✓ |",
    "| `processing` | Processing / On Hold | ✓ |",
    "| `on-hold` | Processing / On Hold | ✓ |",
    "| `in-progress` | In Progress | ✓ |",
    "| `completed` | Completed | — |",
    "| `archived` | Archive | — |",
    "",
    "---",
    "",
    "## RECOMMENDED MODEL FOR THIS PROJECT",
    "",
    "| Task | Model |",
    "|---|---|",
    "| Adding features, refactoring JS | Sonnet 4.6 (`claude-sonnet-4-6`) |",
    "| Architecture decisions, big rewrites | Opus 4.6 (`claude-opus-4-6`) |",
    "| Small edits, CSS tweaks | Haiku 4.5 (`claude-haiku-4-5-20251001`) |",
    "",
    "---",
    "",
    "## FUTURE: BLOCKNOTE MIGRATION",
    "",
    "The notes editor currently uses a styled `contenteditable` div. It is designed to be replaced with **BlockNote** (React-based block editor) in a future migration when this project gets a proper build setup. The editor interface uses `getContent()`/`setContent()`/`onChange()` patterns to keep the swap clean.",
    ""
  ].join("\n"),
  "PHASES.md": [
    "# LBM — Phase Handover Guide",
    "",
    "Each phase ends with a commit + push + the self-contained prompt below it.",
    "Copy the prompt block for that phase into a new Claude Code chat window.",
    "",
    "---",
    "",
    "## ✅ Phase 0 — Completed (this session)",
    "",
    "Full LBM redesign. Commit: `347345f` on `main`.",
    "",
    "What was built:",
    "- New header: abstract kanban SVG logo, LBM branding, Tasks/Docs/Resources nav",
    "- Toolbar: List View / Board View tabs, collapsible search+filter, + New button, meta-row removed",
    "- List view: tabular, hover ✓ Done button, click to open detail panel",
    "- Board view: 6 columns, Notion-style right-side collapse, 3-dot column menu (rename/move/hide/delete), + New item footer, + Add column button",
    "- Task detail panel: slide-in from right, editable title, properties grid, contenteditable rich-text notes (Cmd+B/I/Shift+8), auto-save",
    "- Resources tab: `resources.html` + `resources/` folder",
    "- `CLAUDE.md` + `SKILL.md` added to `TaskTracker/`",
    "",
    "---",
    "",
    "## 🔲 Phase 1 — Bug verification & polish",
    "",
    "**End-of-phase actions:**",
    "```",
    "git add -A && git commit -m \"fix(ltm): phase 1 — bug fixes and UI polish\"",
    "git push",
    "```",
    "Then paste the prompt below into a new chat.",
    "",
    "---",
    "",
    "### PHASE 1 PROMPT — copy everything between the lines",
    "",
    "---",
    "**Workspace:** Open `Local Business Manager/` in Claude Code.",
    "Read `CLAUDE.md` and `SKILL.md` before doing anything.",
    "",
    "**Context:** The LBM (Local Business Manager) was fully redesigned in the previous session (commit `347345f`). It is a vanilla JS/HTML/CSS app — no build step. Open `index.html` in the browser before starting.",
    "",
    "**Your job — fix these known issues one by one, testing in the browser after each:**",
    "",
    "1. **Custom columns + list view** — When a column is added via the `+` button on the board, its `dropLane` key (e.g. `custom-1234567`) is not in `ACTIVE_LANES`, so tasks moved into it disappear from List View. Fix: in `task-app.js` → `confirmAddColumn()`, push the new key into `ACTIVE_LANES` at runtime so it appears in list view.",
    "",
    "2. **Board full-width** — The board sits inside a `max-width: 1400px` `.page-wrap`. Extract `#boardView` outside `.page-wrap` in `index.html` so when Board View is active, the board runs edge-to-edge. The toolbar and list view should stay inside `.page-wrap`. CSS hint: give `#boardView` `padding: 0 24px 48px; width: 100%;` when active.",
    "",
    "3. **Detail panel — lane change** — In the detail panel properties grid, the \"Stage\" row currently shows read-only text. Make it an inline `<select>` so the user can change the stage directly from the detail panel without opening the edit modal. On change → call `moveTask()` and re-render props. See `refreshDetailProps()` in `task-app.js`.",
    "",
    "4. **List view sort controls** — Add a small sort control above the task list (e.g. `Sort: Urgency · Value · Modified`) as text toggles. Default is urgency. Clicking another label re-sorts. Store sort preference in `localStorage` `ui.listSort`.",
    "",
    "5. **Visual QA** — Check that all 3 pages (Tasks, Docs, Resources) look correct: consistent header, no broken links, board columns render at the right width.",
    "",
    "**End this phase by:**",
    "- Running through all 5 fixes in the browser",
    "- `git add -A && git commit -m \"fix(ltm): phase 1 — custom lanes, full-width board, inline stage select, list sort\"`",
    "- `git push`",
    "- Producing the Phase 2 prompt (copy from `PHASES.md` in the repo)",
    "",
    "---",
    "",
    "## 🔲 Phase 2 — UX polish & missing fields",
    "",
    "**End-of-phase actions:**",
    "```",
    "git add -A && git commit -m \"feat(ltm): phase 2 — UX polish, due date, keyboard shortcuts\"",
    "git push",
    "```",
    "",
    "---",
    "",
    "### PHASE 2 PROMPT — copy everything between the lines",
    "",
    "---",
    "**Workspace:** Open `Local Business Manager/` in Claude Code.",
    "Read `CLAUDE.md` and `SKILL.md` before doing anything.",
    "",
    "**Context:** The LBM (Local Business Manager) is a vanilla JS/HTML/CSS task tracker — no build step. Open `index.html` in a browser first. Phase 1 bugs are fixed (custom lanes, full-width board, inline stage select, list sort).",
    "",
    "**Your job:**",
    "",
    "1. **Due date field** — Add `dueDate` (ISO date string, optional) to the task model in `normalizeTask()`. Add a date input to the create/edit modal form. Show it in the detail panel properties grid. In list view, if `dueDate` is set and is today or overdue, show a small coloured date badge next to the task title.",
    "",
    "2. **Keyboard shortcuts legend** — Add a small `?` icon button to the toolbar (next to the filter button). Clicking it shows a compact popover listing: `N` new task · `/` search · `Esc` close · `Cmd+B` bold · `Cmd+I` italic. Style consistent with the col-menu popover.",
    "",
    "3. **Board card value chip** — If `task.value > 0`, show a small `$X,XXX` chip at the bottom of the board card (similar to existing `.board-card-sublane` style). Keep it subtle — muted text, no border.",
    "",
    "4. **Drag handle on board cards** — Replace full-card `draggable` with an explicit `⠿` drag handle (6-dot grid icon, left side of card). Only the handle initiates drag. The rest of the card click opens the detail panel. Update `buildBoardCard()` in `task-app.js`.",
    "",
    "5. **Empty state copy** — Update the empty-state messages to be more useful:",
    "   - List view empty: \"No active tasks. Press N to add one.\"",
    "   - Board column empty: \"Drop a card here or click + New item\"",
    "",
    "**End this phase by:**",
    "- Verifying all 5 items in the browser",
    "- `git add -A && git commit -m \"feat(ltm): phase 2 — due date, keyboard shortcuts, value chip, drag handle\"`",
    "- `git push`",
    "- Producing the Phase 3 prompt (copy from `PHASES.md` in the repo)",
    "",
    "---",
    "",
    "## 🔲 Phase 3 — BlockNote migration",
    "",
    "**End-of-phase actions:**",
    "```",
    "git add -A && git commit -m \"feat(ltm): phase 3 — BlockNote rich text editor\"",
    "git push",
    "```",
    "",
    "---",
    "",
    "### PHASE 3 PROMPT — copy everything between the lines",
    "",
    "---",
    "**Workspace:** Open `Local Business Manager/` in Claude Code.",
    "Read `CLAUDE.md` and `SKILL.md` before doing anything.",
    "",
    "**Context:** The LBM notes editor currently uses a `contenteditable` div. It is designed to be replaced with BlockNote. The swap interface is `getEditorContent()` / `setEditorContent()` in `task-app.js`. `task.body` stores the content.",
    "",
    "**Important:** BlockNote requires React. This phase adds a Vite + React build setup to `TaskTracker/`. The existing `index.html`, `styles.css`, `task-app.js` stay as-is except the detail panel notes section gets swapped. Keep the build output separate so the app still opens as `index.html` for non-React parts OR do a full React migration if cleaner.",
    "",
    "**Recommended approach (hybrid — minimal change):**",
    "1. Create `TaskTracker/editor/` as a separate mini React app (Vite):",
    "   ```",
    "   cd TaskTracker && npm create vite@latest editor -- --template react",
    "   cd editor && npm install @blocknote/core @blocknote/react @blocknote/mantine",
    "   ```",
    "2. The editor app exposes a custom element / iframe API:",
    "   - Receives content via `postMessage({ type: \"set\", content: [...blocks] })`",
    "   - Sends changes via `postMessage({ type: \"change\", content: [...blocks] })`",
    "3. In `task-app.js`, replace `#notesEditor` with an `<iframe src=\"editor/dist/index.html\">` and update `getEditorContent()` / `setEditorContent()` to use postMessage.",
    "4. Style the BlockNote editor to match the dark theme (override BlockNote CSS variables).",
    "5. `editor/dist/` should be built and committed. Add `editor/node_modules/` to `.gitignore`.",
    "",
    "**Storage format change:** BlockNote stores content as a JSON block array, not HTML. Update `task.body` to store this JSON string. Update `plainPreview()` in `task-app.js` to extract text from the block JSON for list/board previews.",
    "",
    "**End this phase by:**",
    "- Testing: create task → open detail → type rich content → close → reopen → content persists",
    "- `git add -A && git commit -m \"feat(ltm): phase 3 — BlockNote rich text editor via iframe bridge\"`",
    "- `git push`",
    "- Producing the Phase 4 prompt (copy from `PHASES.md` in the repo)",
    "",
    "---",
    "",
    "## 🔲 Phase 4 — Portability & multi-project setup",
    "",
    "**End-of-phase actions:**",
    "```",
    "git add -A && git commit -m \"feat(ltm): phase 4 — portable config, setup script\"",
    "git push",
    "```",
    "",
    "---",
    "",
    "### PHASE 4 PROMPT — copy everything between the lines",
    "",
    "---",
    "**Workspace:** Open `Local Business Manager/` in Claude Code.",
    "Read `CLAUDE.md` and `SKILL.md` before doing anything.",
    "",
    "**Context:** The LBM is designed to be dropped into any project repo. Right now the project name, areas, and seed tasks are hardcoded in `project-data.js`. This phase makes it fully portable.",
    "",
    "**Your job:**",
    "",
    "1. **`ltm.config.json`** — Extract project identity out of `project-data.js` into a new `TaskTracker/ltm.config.json`:",
    "   ```json",
    "   {",
    "     \"name\": \"LBM\",",
    "     \"fullName\": \"Mac Control Center\",",
    "     \"storageKey\": \"mac-control-center-task-tracker-v1\",",
    "     \"areas\": [\"project-system\", \"docs\", \"product\", \"platform\", \"release\", \"security\", \"ui-ux\"],",
    "     \"seedVersion\": \"2026-03-30-r2\"",
    "   }",
    "   ```",
    "   Load this in `project-data.js` via a `fetch(\"ltm.config.json\")` at startup (or inline it — your call based on what works without a build step).",
    "",
    "2. **`setup.sh`** — Write a shell script at `TaskTracker/setup.sh` that copies the LBM into a target repo:",
    "   ```bash",
    "   ./setup.sh /path/to/target-repo \"My Project Name\"",
    "   ```",
    "   It should: copy `TaskTracker/` into the target, replace the name in `ltm.config.json`, clear the seed tasks (leave a blank example), and print next steps.",
    "",
    "3. **README update** — Rewrite `TaskTracker/README.md` to be the canonical \"how to use LBM in your project\" guide. Sections: What is LBM · Running locally · Customising (name, areas, seed tasks) · Adding docs · Column management · Notes on the build exclusion.",
    "",
    "4. **`.gitignore` update** — At the root of the Mac Control Centre repo, ensure these are gitignored (if not already): `TaskTracker/editor/node_modules/`, `TaskTracker/editor/dist/` is committed (not ignored).",
    "",
    "**End this phase by:**",
    "- `git add -A && git commit -m \"feat(ltm): phase 4 — ltm.config.json, setup.sh, README rewrite\"`",
    "- `git push`",
    "",
    "---",
    "",
    "## Notes for all phases",
    "",
    "- **Workspace to open:** `0A. Mac Control Centre/TaskTracker/` (or the parent `0A. Mac Control Centre/`)",
    "- **No build step** until Phase 3. Open `index.html` directly in browser.",
    "- **Test after every change** — open browser, check list view, board view, detail panel.",
    "- **Commit format:** `type(ltm): phase N — description`",
    "- **Branch:** work on `main` unless the phase introduces breaking changes (then use a feature branch)",
    "- **`CLAUDE.md`** inside `TaskTracker/` bootstraps Claude Code context automatically when that folder is the workspace.",
    ""
  ].join("\n"),
  "README.md": [
    "# Local Business Manager (LBM)",
    "",
    "**LBM** is a lightweight, self-contained, vanilla JavaScript task and project manager designed for developers and creators. It is a local-first alternative to Notion that requires zero installation, no backend, and no build step. Drop this folder into any Git repository and open `index.html` — it works immediately.",
    "",
    "---",
    "",
    "## The Vision",
    "",
    "As AI matures, how we consume business tools is changing.",
    "",
    "> *\"With AI, we don't just buy coaches or tools separately — we buy everything in a box.\"*",
    "",
    "LBM is the foundational tier of a three-part vision:",
    "",
    "| Tier | Name | Status | Audience |",
    "|---|---|---|---|",
    "| 1 | **LBM** — Local Business Manager | Open Source / Free | Developers, solo creators |",
    "| 2 | **OBM** — Online Business Manager | Freemium SaaS (planned) | Entrepreneurs, startups |",
    "| 3 | **Business in a Box** | Premium agency rollout (planned) | Corporations, scaling teams |",
    "",
    "---",
    "",
    "## Quick Start",
    "",
    "1. **Copy this folder** into your project (or clone the repo)",
    "2. **Open `index.html`** in any modern browser (Chrome, Firefox, Safari, Edge)",
    "3. **Start using it** — sample tasks are pre-loaded so you can see everything working",
    "",
    "That is it. No npm, no build step, no sign-up.",
    "",
    "**Full setup guide:** docs/SETUP_GUIDE.md",
    "",
    "---",
    "",
    "## Customise for Your Project",
    "",
    "All customisation happens in `data/project-data.js`:",
    "- Set your project name and areas",
    "- Define your seed task backlog",
    "- Add entries to the docs and skills index",
    "",
    "---",
    "",
    "## Handing Off to Someone Else",
    "",
    "1. Commit your current `data/project-data.js` with the right seed tasks and areas",
    "2. Send them the folder (zip, git clone, or copy)",
    "3. They open `index.html` — done",
    "",
    "New users always start from the committed seed data. Point them at docs/SETUP_GUIDE.md.",
    ""
  ].join("\n"),
  "SKILL.md": [
    "# SKILL.md — LBM Development Guide",
    "",
    "How to develop and extend the Local Business Manager.",
    "",
    "---",
    "",
    "## RUNNING THE APP",
    "",
    "No build step. Open `index.html` directly in a browser:",
    "",
    "```bash",
    "open index.html",
    "```",
    "",
    "Or serve locally to avoid CORS issues with doc file loading:",
    "",
    "```bash",
    "cd TaskTracker && python3 -m http.server 8080",
    "# Open http://localhost:8080",
    "```",
    "",
    "---",
    "",
    "## ADDING A NEW FEATURE",
    "",
    "1. **New task field** → update `normalizeTask()` in `task-app.js` to add the field with a default, add it to the modal form in `index.html`, and handle it in `handleSubmit()`.",
    "2. **New board column** → update `DEFAULT_BOARD_COLUMNS` in `task-app.js` and add the lane key to `ALL_LANES` / `ACTIVE_LANES` as appropriate.",
    "3. **New page** → copy the header from `docs.html`, add the tab to all 3 pages (index, docs, resources), import `styles.css` and `project-data.js`.",
    "4. **New doc** → add an entry to the `docs` array in `project-data.js`. The docs viewer picks it up automatically.",
    "",
    "---",
    "",
    "## SEED DATA",
    "",
    "Task seed data lives in `project-data.js` under `window.MCCProjectData.tracker.tasks`. To reset a browser to the latest seed: open the info panel (ⓘ button) and click \"Reset to Seed\".",
    "",
    "---",
    "",
    "## UPDATING STYLES",
    "",
    "All styles live in `styles.css`. Design tokens are CSS variables at the top (`:root`). The dark theme uses:",
    "- `--bg` `--surface` `--border` for backgrounds",
    "- `--accent` (purple) and `--done` (teal) for action colours",
    "- `--text` `--muted` `--muted-soft` for text hierarchy",
    "",
    "---",
    "",
    "## NOTES EDITOR (contenteditable)",
    "",
    "The task detail panel uses a `contenteditable` div (`#notesEditor`). Content is saved as HTML into `task.body`. The plain-text preview in list/board rows reads `task.notes`.",
    "",
    "**Future migration to BlockNote:**",
    "When this project moves to a React + Vite setup, replace the `#notesEditor` div and its binding in `task-app.js` with a BlockNote component. The `getEditorContent()` / `setEditorContent()` helpers make the swap straightforward.",
    "",
    "---",
    "",
    "## RESOURCES FOLDER",
    "",
    "Place design assets, exported icons, or reference files in `resources/`. Update `resources.html` to list new items. These files are committed to git but not bundled into any build output.",
    ""
  ].join("\n"),
  "docs/LOCAL_PROJECT_SYSTEM.md": [
    "# Local Project System",
    "",
    "## Goal",
    "",
    "Keep project management and documentation close to the codebase so AI agents and humans can update them in the same workspace, without needing a separate tool.",
    "",
    "---",
    "",
    "## Components",
    "",
    "### `index.html`",
    "",
    "The main app. Local-first task board for backlog review, quick edits, filtering, export, and manual prioritisation. Open this directly in the browser — no server required.",
    "",
    "### `docs.html`",
    "",
    "The documentation viewer. Lists all docs and skill files registered in `data/project-data.js`. Click any entry to read the rendered markdown.",
    "",
    "### `data/project-data.js`",
    "",
    "Repo-owned seed data for:",
    "",
    "- Project metadata (name, areas)",
    "- The seed task backlog",
    "- The docs index (what appears in the Docs tab)",
    "- The skill index (what appears in the Skills section)",
    "",
    "### `data/docs-content.js`",
    "",
    "A pre-rendered cache of all markdown doc files. The docs viewer reads from this cache rather than fetching raw files — this avoids CORS issues when opening `docs.html` directly as a file. **Always keep this in sync with the `.md` files.**",
    "",
    "### `docs/`",
    "",
    "Markdown source files for project guides, architecture notes, and workflow docs. These are the source of truth — `docs-content.js` is derived from them.",
    "",
    "---",
    "",
    "## How to Add Tasks",
    "",
    "### Durable, repo-owned tasks",
    "",
    "Add them to the `tasks` array in `data/project-data.js` and commit. Everyone who opens the app fresh will see these tasks.",
    "",
    "### Personal working notes",
    "",
    "Add them inside the browser UI. They are stored in `localStorage` on that machine only.",
    "",
    "---",
    "",
    "## AI-Recommended Tasks",
    "",
    "When an AI assistant recommends a task for the backlog, mark it with:",
    "",
    "```js",
    "source: \"recommended\",",
    "recommendedBy: \"Claude Sonnet 4.6\"",
    "```",
    "",
    "This keeps human-requested work distinct from AI-suggested work and makes the provenance traceable in the task list.",
    "",
    "---",
    "",
    "## How to Add a New Doc",
    "",
    "1. Create the `.md` file in `docs/`",
    "2. Add an entry to the `docs` array in `data/project-data.js`",
    "3. Add the cached content to `data/docs-content.js`",
    "",
    "The Docs tab will pick it up automatically. See AI Development Guide for the full step-by-step.",
    "",
    "---",
    "",
    "## Online Sync Model",
    "",
    "The local tracker is the fast workspace — not an automatic sync engine. For cross-device or team visibility:",
    "",
    "1. Update repo-owned tasks in `data/project-data.js` and commit",
    "2. Or export JSON/Markdown from the app and import into an online tool",
    "",
    "If online sync becomes important, write a small sync script against the chosen service rather than trying to make static HTML write directly to repo files.",
    ""
  ].join("\n"),
  "docs/PERSISTENCE_AND_STATE.md": [
    "# Persistence and State",
    "",
    "The Local Business Manager has two persistence layers. Understanding them prevents confusion about where your data is and why changes may or may not be visible to others.",
    "",
    "---",
    "",
    "## 1. Seed Data (Git-tracked Baseline)",
    "",
    "### Stored In",
    "",
    "- `data/project-data.js` — committed to the git repository",
    "",
    "### Contains",
    "",
    "- Project metadata (name, areas, seed version)",
    "- The initial/baseline backlog of tasks",
    "- The docs index (which files appear in the Docs tab)",
    "- The skills index (which skill files appear in the Skills section)",
    "",
    "### Sync Behaviour",
    "",
    "- Anyone who clones or copies the repo gets this data",
    "- Changes require editing `data/project-data.js` and committing",
    "- These are the \"official\" tasks — the source of truth for the project baseline",
    "",
    "---",
    "",
    "## 2. Browser Local State (Per-machine, Per-browser)",
    "",
    "### Stored In",
    "",
    "- `localStorage` in the user's browser",
    "- Storage key: `ltm-task-tracker-v1`",
    "",
    "### Contains",
    "",
    "- All tasks created or edited in the browser UI after the initial load",
    "- UI preferences: collapsed board columns (`ui.collapsedColumns`), list sort (`ui.listSort`)",
    "- Confirm-dialog preferences (e.g. `lbm_skipDeleteConfirm`)",
    "- The editable project title (if changed via the header)",
    "",
    "### Sync Behaviour",
    "",
    "- Completely local to that browser on that machine",
    "- Not committed to git automatically",
    "- Not shared across devices or users",
    "- Survives page refreshes within the same browser",
    "- Lost if the user clears browser data or opens the app in a different browser",
    "",
    "---",
    "",
    "## What Happens on First Load",
    "",
    "When a user opens `index.html` for the first time in a new browser:",
    "",
    "1. No `localStorage` data exists yet",
    "2. The app reads `data/project-data.js` and loads the seed tasks",
    "3. From that point on, changes are saved to `localStorage`",
    "",
    "The seed data is **only loaded once** per browser (on first run or after a reset).",
    "",
    "---",
    "",
    "## Resetting to Seed",
    "",
    "To wipe local changes and reload from the git-tracked baseline:",
    "",
    "1. Click the **ⓘ** info button in the app header",
    "2. Click **Reset to Seed**",
    "",
    "This clears the `localStorage` task data and reloads the seed tasks from `data/project-data.js`.",
    "",
    "---",
    "",
    "## Sharing Changes Across Machines",
    "",
    "Because browser state is local, sharing requires an explicit step:",
    "",
    "| Goal | How |",
    "|---|---|",
    "| Share a task with a teammate | Export JSON → send the file → they import it |",
    "| Make a task permanent for all users | Edit `data/project-data.js` and commit |",
    "| Back up your local work | Export JSON or Markdown from the ⓘ info panel |",
    "",
    "---",
    "",
    "## Practical Summary",
    "",
    "> Add tasks in the browser for personal working notes.",
    "> Commit changes to `data/project-data.js` when a task should be visible to everyone who opens the app fresh.",
    ""
  ].join("\n"),
  "docs/VISION_AND_PHILOSOPHY.md": [
    "# Vision and Philosophy",
    "",
    "## The Philosophy: \"Everything in a Box\"",
    "The modern era of artificial intelligence forces us to rethink how we distribute value. Historically, businesses had to purchase software tools, hire business coaches, and buy educational courses entirely separate from one another. ",
    "",
    "We envision a reality where you **do not buy coaches, clients, or disjointed tools.** Instead, you buy **\"Everything in a box.\"**",
    "",
    "Our software aims to provide the tool, the coach, the operational framework, the tutorial, and the exact implementation steps all packaged together simultaneously.",
    "",
    "---",
    "",
    "## The Three Tiers of the Ecosystem",
    "",
    "We are constructing this reality through a three-tier system:",
    "",
    "### Tier 1: Local Business Manager (LBM)",
    "* **Status:** Open Source / Free Forever",
    "* **Audience:** Developers, solo-creators, and technical teams.",
    "* **Concept:** A Notion-alternative that lives purely on your local machine. You can copy the code directly into any local development environment, edit `project-data.js`, and instantly have a blazing-fast, local-first task and project management interface. No databases, no subscriptions, no bloat.",
    "",
    "### Tier 2: Online Business Manager (OBM)",
    "* **Status:** Freemium SaaS",
    "* **Audience:** General public, entrepreneurs, and startups.",
    "* **Concept:** A hosted, cloud-based counterpart to LBM. Users onboard through a specialized, gamified questionnaire. Based on their answers, the system automatically spins up a structured, customized business plan and task board. Users pay a small subscription fee for cloud-sync, ongoing content updates, and expanded capabilities at a fraction of the cost of complex enterprise tools.",
    "",
    "### Tier 3: \"Business in a Box\" Agency Rollout",
    "* **Status:** Premium Proprietary Implementation",
    "* **Audience:** Established businesses, corporations, and teams scaling their operations.",
    "* **Concept:** This is the highest level of our implementation. Clients purchase the OBM as an enterprise module. It comes pre-loaded with proprietary content, training videos, and a dedicated organizational framework designed to systemize the CEO and structure communication across the team. As they execute their goals, our tool drives the highest and best innovations directly into their company step-by-step.",
    ""
  ].join("\n"),

  "docs/KEYBOARD_SHORTCUTS.md": [
    "# Keyboard Shortcuts",
    "",
    "> Shortcuts are active when no text field, textarea, or notes editor is focused.",
    "> `Ctrl+N` and `Escape` always work regardless of focus.",
    "",
    "---",
    "",
    "## Global",
    "",
    "| Action | Key |",
    "|---|---|",
    "| New item | `N` |",
    "| New item — works even while a field is focused | `Ctrl+N` / `Cmd+N` |",
    "| Focus search bar | `/` |",
    "| Toggle shortcuts panel | `?` |",
    "| Close any modal, panel, or form | `Esc` |",
    "| Undo (delete · lane change · title edit) | `Cmd+Z` / `Ctrl+Z` |",
    "",
    "---",
    "",
    "## List View",
    "",
    "### Navigation",
    "",
    "| Action | Key |",
    "|---|---|",
    "| Focus next task | `↓` |",
    "| Focus previous task | `↑` |",
    "| Scroll page down | `Shift+↓` |",
    "| Scroll page up | `Shift+↑` |",
    "| Open focused task | `Enter` |",
    "| Delete focused task (Cmd+Z to undo) | `D` |",
    "| Jump to last deleted position | `←` / `→` |",
    "| Clear row focus | `Esc` |",
    "",
    "---",
    "",
    "## Detail Panel",
    "",
    "| Action | Key |",
    "|---|---|",
    "| Delete this task (Cmd+Z to undo) | `D` |",
    "| Close panel | `Esc` |",
    "",
    "### Notes Editor",
    "",
    "| Action | Key |",
    "|---|---|",
    "| Bold | `Cmd+B` / `Ctrl+B` |",
    "| Italic | `Cmd+I` / `Ctrl+I` |",
    "| Bullet list | `Cmd+Shift+8` / `Ctrl+Shift+8` |",
    "",
    "---",
    "",
    "## Board View",
    "",
    "### Inline Form",
    "",
    "| Action | Key |",
    "|---|---|",
    "| Save task | `Enter` |",
    "| Cancel | `Esc` |"
  ].join("\n"),

  "DESIGN_SKILL.md": [
    "# DESIGN_SKILL.md — LBM Front-End Design Reference",
    "",
    "This file is the canonical design reference for the Local Business Manager project.",
    "Read it in full before doing any CSS, component, or UI work.",
    "",
    "---",
    "",
    "## 1. When to Read This File",
    "",
    "Read this file before:",
    "- Writing or editing CSS in `styles.css`",
    "- Adding or modifying any component's HTML structure",
    "- Choosing colors, spacing values, font sizes, or border radii",
    "- Adding new animations or transitions",
    "- Creating a new UI pattern that doesn't have a clear existing precedent",
    "",
    "This file overrides generic advice. When this document and intuition conflict, follow this document.",
    "",
    "---",
    "",
    "## 2. LBM Design Philosophy",
    "",
    "These principles are drawn from the best task/project management UIs in the industry (Linear, Vercel, Cron) and adapted to this project.",
    "",
    "**Density-first.** Information should be visible without scrolling. Linear's principle: show more, not less. Every pixel of vertical space is precious. Avoid generous padding that hides content.",
    "",
    "**Chrome should disappear.** UI furniture — borders, backgrounds, labels — should recede so content is the focus. If a border, shadow, or background isn't communicating something, remove it.",
    "",
    "**One accent color, used sparingly.** `--accent` purple is the only interactive signal. It appears on active states, focus rings, and primary buttons. Never use it decoratively. When everything is purple, nothing is.",
    "",
    "**Dark-only.** All decisions optimize for dark backgrounds. Never add `prefers-color-scheme: light` rules or light-mode variants unless explicitly requested.",
    "",
    "**Keyboard-native.** Every action must be reachable by keyboard. This is a task manager for power users. Tab order, focus states, and keyboard shortcuts are first-class features, not afterthoughts.",
    "",
    "**No loading states.** This is a local-first app. No spinners, skeletons, or fake latency patterns. State changes are instant.",
    "",
    "**Never animate to fill time.** Animation exists to guide attention or communicate state change. If removing an animation makes the UI feel faster without losing clarity, remove it.",
    "",
    "---",
    "",
    "## 3. Color System",
    "",
    "### Current Design Tokens",
    "",
    "All colors live in `:root` in `styles.css`. Never use hardcoded hex values outside of `:root`.",
    "",
    "| Variable | Value | Purpose |",
    "|---|---|---|",
    "| `--bg` | `#0f1016` | Page background — deepest layer |",
    "| `--surface` | `rgba(22,23,34,0.75)` | Glassmorphic card surfaces |",
    "| `--surface-solid` | `#161722` | Opaque variant for panels needing full opacity |",
    "| `--surface-hover` | `rgba(38,39,55,0.85)` | Surface hover state |",
    "| `--text` | `#f8f9fa` | Primary text |",
    "| `--muted` | `#a1a1aa` | Secondary text, labels |",
    "| `--muted-soft` | `#71717a` | Tertiary text, placeholders |",
    "| `--accent` | `#8b5cf6` | Primary interactive color (purple) |",
    "| `--accent-glow` | `rgba(139,92,246,0.4)` | Shadows and glows on accent |",
    "| `--accent-hover` | `#a78bfa` | Accent hover state |",
    "| `--accent-soft` | `rgba(139,92,246,0.12)` | Very soft accent background tint |",
    "| `--done` | `#34d399` | Success / completed state (green) |",
    "| `--warn` | `#fbbf24` | Warning state (amber) |",
    "| `--danger` | `#ef4444` | Destructive / error state (red) |",
    "| `--border` | `rgba(255,255,255,0.08)` | Default border |",
    "| `--border-strong` | `rgba(255,255,255,0.13)` | Emphasized border (hover, active) |",
    "",
    "Extended tokens from `design-tokens.css`:",
    "",
    "| Variable | Value | Purpose |",
    "|---|---|---|",
    "| `--surface-deep` | `#13141f` | Deepest panel backgrounds (detail panel) |",
    "| `--surface-overlay` | `#1c1d28` | Floating overlays (shortcuts panel) |",
    "| `--accent-light` | `#c4b5fd` | Accent-tinted text on dark surfaces |",
    "| `--text-secondary` | `#d4d4d8` | Between `--text` and `--muted` |",
    "",
    "### What NOT to Do",
    "",
    "Never write a hex value directly inside a CSS selector. All colors must use `var(--token-name)`.",
    "",
    "Known violations to fix (Tier 2): `#13141f` → `var(--surface-deep)`, `#1c1d28` → `var(--surface-overlay)`, `#c4b5fd` → `var(--accent-light)`.",
    "",
    "### OKLCH — Future Path",
    "",
    "OKLCH is supported in all modern browsers (Chrome 111+, Safari 15.4+, Firefox 113+). OKLCH equivalents for reference:",
    "- `#8b5cf6` (accent) → `oklch(60% 0.22 293)`",
    "- `#0f1016` (bg) → `oklch(8% 0.01 264)`",
    "- `#34d399` (done) → `oklch(78% 0.15 162)`",
    "",
    "---",
    "",
    "## 4. Typography Scale",
    "",
    "8 steps based on a Minor Third ratio (×1.2), base at 14px. Never introduce a font size outside this scale.",
    "",
    "| Token | rem | px | Typical use |",
    "|---|---|---|---|",
    "| `--text-2xs` | 0.625rem | 10px | Tiny badges, count bubbles |",
    "| `--text-xs` | 0.6875rem | 11px | Section labels, ALL-CAPS metadata |",
    "| `--text-sm` | 0.75rem | 12px | Chip text, property labels, timestamps |",
    "| `--text-base` | 0.875rem | 14px | Body text, list items, form labels |",
    "| `--text-md` | 1rem | 16px | Card titles, subheadings |",
    "| `--text-lg` | 1.125rem | 18px | Panel section titles |",
    "| `--text-xl` | 1.25rem | 20px | Stat numbers, brand name |",
    "| `--text-2xl` | 1.5rem | 24px | Detail panel H1, page titles |",
    "",
    "**Line height rules:** Labels / chips: `1.2` — Body text: `1.5` — Rich text / notes: `1.75` — Headings: `1.15`",
    "",
    "**Weight rules:** 400 = body — 500 = interactive labels — 600 = section headers, button labels — 700 = page H1, stat numbers",
    "",
    "**Letter spacing:** Uppercase micro-labels: `0.06em–0.09em` — Display text: `-0.02em` — Body text: `0`",
    "",
    "---",
    "",
    "## 5. Spacing System",
    "",
    "The base unit is **4px**. Every spacing value must be a multiple of 4. Valid: 4, 8, 12, 16, 20, 24, 32, 40, 48px.",
    "",
    "| Token | Value |",
    "|---|---|",
    "| `--space-1` | 4px |",
    "| `--space-2` | 8px |",
    "| `--space-3` | 12px |",
    "| `--space-4` | 16px |",
    "| `--space-5` | 20px |",
    "| `--space-6` | 24px |",
    "| `--space-8` | 32px |",
    "| `--space-10` | 40px |",
    "| `--space-12` | 48px |",
    "",
    "All interactive elements must be at least **44×44px** on touch (Apple HIG, WCAG 2.5.5).",
    "",
    "---",
    "",
    "## 6. Component Patterns",
    "",
    "### Buttons — 4 Tiers",
    "",
    "| Tier | When to use |",
    "|---|---|",
    "| Primary | One primary action per view. Never more than one per form/group. |",
    "| Secondary | Alternative actions. Cancel, Edit. |",
    "| Ghost | Tertiary, contextual. Icon buttons, inline tools. |",
    "| Danger | Irreversible destructive actions only. Confirm before executing. |",
    "",
    "Min height: 32px compact / 36px normal. Focus state: `box-shadow: 0 0 0 2px var(--accent-glow), 0 0 0 1px var(--accent)` on `:focus-visible`.",
    "",
    "### Cards and Surfaces",
    "",
    "| Surface | Token | Use |",
    "|---|---|---|",
    "| Glass card | `--surface` | Primary cards in list/board views |",
    "| Solid panel | `--surface-solid` | Panels needing full opacity |",
    "| Deep panel | `--surface-deep` | Detail panel, deepest content layer |",
    "| Overlay | `--surface-overlay` | Floating overlays, shortcuts panel |",
    "",
    "Radius: `--radius-md` (10px) for cards, `--radius-lg` (14px) for modals/panels. Never stack more than 2 levels of glass.",
    "",
    "### Inputs",
    "",
    "Focus: `border-color: var(--accent)` + `box-shadow: 0 0 0 3px rgba(139,92,246,0.1)`. Error state: swap to `var(--danger)` equivalent. Min height: 36px.",
    "",
    "---",
    "",
    "## 7. Motion and Animation",
    "",
    "| Duration | Use case |",
    "|---|---|",
    "| 80–100ms | Hover color/background changes |",
    "| 120–150ms | Icon reveals, opacity fades |",
    "| 200ms | Component entry (badge appearing) |",
    "| 220–280ms | Panel slides, modal enters |",
    "| 300ms | Tab indicator slide |",
    "| Never >400ms | — |",
    "",
    "| Easing | Token | When |",
    "|---|---|---|",
    "| `cubic-bezier(0.4, 0, 0.2, 1)` | `--ease-standard` | Position/transform changes |",
    "| `cubic-bezier(0.4, 0, 1, 1)` | `--ease-exit` | Exit animations |",
    "| `cubic-bezier(0, 0, 0.2, 1)` | `--ease-enter` | Entry animations |",
    "",
    "**What NOT to animate:** layout shifts (height/width), `filter: blur()` on large surfaces, color changes >150ms, list entries on >10 items.",
    "",
    "`prefers-reduced-motion` is handled globally in `design-tokens.css` — do not add redundant blocks elsewhere.",
    "",
    "---",
    "",
    "## 8. Accessibility",
    "",
    "**WCAG 2.1 AA:** Body text ≥ 4.5:1 contrast. Large text ≥ 3:1. `--muted-soft` (#71717a on #0f1016) is ~4.55:1 — do not darken further.",
    "",
    "**Standard focus ring** for all interactive elements on `:focus-visible`:",
    "",
    "```",
    ":focus-visible {",
    "  outline: none;",
    "  box-shadow: 0 0 0 2px var(--accent-glow), 0 0 0 1px var(--accent);",
    "}",
    "```",
    "",
    "**Known focus violations (Tier 1):**",
    "- `.brand-name:focus-visible` — currently kills keyboard nav",
    "- `.search-input:focus-visible` — same",
    "- `.detail-prop-select:focus` — outline: none with no substitute",
    "- All `.icon-button` elements — missing `:focus-visible` rules",
    "",
    "Use `.sr-only` (defined in `design-tokens.css`) for labels on icon-only controls and off-screen context.",
    "",
    "---",
    "",
    "## 9. Reference Sources",
    "",
    "| Source | What to study |",
    "|---|---|",
    "| Linear.app | Row density, keyboard-first patterns, minimal chrome, dark mode done right |",
    "| Vercel Dashboard | Card system, dark mode surfaces, info density, status chip patterns |",
    "| shadcn/ui | Component variants, focus ring patterns, accessible HTML structure |",
    "| Radix UI | Modal/dialog/popover ARIA patterns, accessible dropdown implementation |",
    "| Material Design 3 | Color roles, spacing system, elevation model |",
    "| Apple HIG | Touch targets, motion guidelines, accessibility |",
    "| WCAG 2.1 Quick Ref | Pass/fail criteria for any accessibility question |",
    "",
    "---",
    "",
    "## 10. Improvement Checklist",
    "",
    "### Tier 1 — Critical (accessibility and correctness)",
    "",
    "- [ ] Fix `.brand-name:focus-visible` — replace `outline: none; box-shadow: none` with standard focus ring",
    "- [ ] Fix `.search-input:focus-visible` — same fix",
    "- [ ] Fix `.detail-prop-select:focus` — add `box-shadow` focus ring",
    "- [ ] Add `:focus-visible` rules to all `.icon-button` elements",
    "- [ ] Add `role=\"dialog\"`, `aria-modal=\"true\"`, `aria-labelledby` to delete confirm overlay",
    "- [ ] Audit all icon-only buttons for `aria-label` presence",
    "",
    "### Tier 2 — Important (design consistency)",
    "",
    "- [ ] Replace `#13141f` with `var(--surface-deep)` in detail panel styles",
    "- [ ] Replace `#1c1d28` with `var(--surface-overlay)` in shortcuts panel styles",
    "- [ ] Replace `#c4b5fd` with `var(--accent-light)` in `.markdown-view h3`",
    "- [ ] Begin migrating font sizes to the 8-step type scale",
    "- [ ] Begin using spacing tokens (`--space-*`) in new CSS rules",
    "- [ ] Replace hardcoded `border-radius: 4px` with `var(--radius-xs)`",
    "- [ ] Fix known spacing violations: 11px → 12px, 7px → 8px, etc.",
    "",
    "### Tier 3 — Polish (nice-to-have)",
    "",
    "- [ ] Increase touch targets for `.icon-button` and `.detail-back-btn` to 44px minimum",
    "- [ ] Add medium breakpoint at `1024px` for tablet layouts",
    "- [ ] Use OKLCH for any new color additions",
    "- [ ] Audit decorative SVG icons for missing `aria-hidden=\"true\"`",
    "- [ ] Add focus trap logic to modals with form inputs"
  ].join("\n"),

  "docs/SETUP_GUIDE.md": [
    "# Setup Guide",
    "",
    "This guide gets you from zero to a working Local Business Manager installation in under five minutes — no command line required.",
    "",
    "---",
    "",
    "## Prerequisites",
    "",
    "- A modern web browser (Chrome, Firefox, Safari, or Edge)",
    "- A text editor if you want to customise the project name or seed tasks (VS Code, Notepad, TextEdit — anything works)",
    "",
    "That is it. No Node.js, no npm, no installation.",
    "",
    "---",
    "",
    "## Step 1 — Get the Files",
    "",
    "**Option A: Copy the folder**",
    "Copy the entire `1_LBM_Local_Business_Manager/` folder into your own project directory or anywhere on your computer.",
    "",
    "**Option B: Clone from Git**",
    "```bash",
    "git clone <repo-url>",
    "```",
    "",
    "**Option C: Download as ZIP**",
    "Download the repository as a ZIP file from GitHub and extract it anywhere.",
    "",
    "---",
    "",
    "## Step 2 — Open the App",
    "",
    "Navigate into the folder and double-click `index.html` to open it in your browser.",
    "",
    "> **Tip:** If doc files in the Docs tab fail to load, serve the folder with a local server instead:",
    "> ```bash",
    "> cd 1_LBM_Local_Business_Manager",
    "> python3 -m http.server 8080",
    "> ```",
    "> Then open `http://localhost:8080` in your browser.",
    "",
    "---",
    "",
    "## Step 3 — Customise the Project Name",
    "",
    "Open `data/project-data.js` in a text editor and update the top section:",
    "",
    "```js",
    "project: {",
    "  name: \"LBM\",                        // ← short name shown in the header",
    "  fullName: \"Local Business Manager\", // ← full name for display",
    "  maintainedBy: \"Your Name\",",
    "}",
    "```",
    "",
    "Save the file and refresh your browser. The header title updates immediately.",
    "",
    "---",
    "",
    "## Step 4 — Define Your Working Areas",
    "",
    "Still in `data/project-data.js`, find the `areas` array and replace the defaults with your own categories:",
    "",
    "```js",
    "areas: [",
    "  \"design\",",
    "  \"development\",",
    "  \"marketing\",",
    "  \"operations\"",
    "],",
    "```",
    "",
    "These areas appear as filter options in the task list.",
    "",
    "---",
    "",
    "## Step 5 — Set Your Seed Tasks",
    "",
    "The `tasks` array in `data/project-data.js` contains the default tasks loaded when someone opens the app for the first time (or after a reset).",
    "",
    "Replace the sample tasks with your own starting backlog, or clear the array entirely for a blank slate:",
    "",
    "```js",
    "tasks: []",
    "```",
    "",
    "> **What is seed data?** Seed tasks are the git-committed baseline. Tasks you create in the browser UI are saved to `localStorage` and are personal to that browser. See Persistence and State for the full explanation.",
    "",
    "---",
    "",
    "## Resetting to Seed Data",
    "",
    "If you want to wipe your browser's local changes and start fresh from the seed data:",
    "",
    "1. Click the **ⓘ** (info) icon in the app header",
    "2. Click **Reset to Seed**",
    "",
    "---",
    "",
    "## Exporting Your Tasks",
    "",
    "To back up or share your current task list:",
    "",
    "1. Open the **ⓘ** info panel",
    "2. Click **Export JSON** or **Export Markdown**",
    "",
    "---",
    "",
    "## Handing Off to a New User",
    "",
    "When giving this app to someone else:",
    "",
    "1. Make sure `data/project-data.js` has the correct seed tasks and areas committed to git",
    "2. Tell them to open `index.html` in their browser",
    "3. Point them to this guide and the Docs tab for further reading",
    "",
    "New users always start from the seed data the first time they open the app in their browser.",
    "",
    "---",
    "",
    "## Troubleshooting",
    "",
    "| Problem | Fix |",
    "|---|---|",
    "| Docs tab shows empty or broken content | Serve with `python3 -m http.server 8080` instead of opening the file directly |",
    "| My tasks disappeared | Check if you opened the file in a different browser or cleared browser data. Tasks live in `localStorage`. |",
    "| Header shows wrong project name | Update `data/project-data.js` and refresh |",
    "| I want to start fresh | Use the **Reset to Seed** button in the ⓘ info panel |",
    ""
  ].join("\n"),

  "docs/AI_DEVELOPMENT_GUIDE.md": [
    "# AI Development Guide",
    "",
    "How to develop the Local Business Manager efficiently using Claude Code. This guide covers token conservation, the right model for each task type, and the conventions that keep AI-assisted work clean and consistent.",
    "",
    "---",
    "",
    "## Recommended Model",
    "",
    "| Task | Model | Model ID |",
    "|---|---|---|",
    "| New feature, refactoring JS | Sonnet 4.6 | `claude-sonnet-4-6` |",
    "| Architecture decisions, big rewrites | Opus 4.6 | `claude-opus-4-6` |",
    "| CSS tweaks, small edits, doc updates | Haiku 4.5 | `claude-haiku-4-5-20251001` |",
    "",
    "Switch with `/model <model-id>` in Claude Code.",
    "",
    "**Default to Sonnet.** Only escalate to Opus when the task genuinely requires multi-file reasoning or architectural judgment.",
    "",
    "---",
    "",
    "## Session Start Protocol",
    "",
    "Every new Claude Code session in this folder should begin by reading:",
    "",
    "1. `CLAUDE.md` — session bootstrap, file map, design rules",
    "2. `SKILL.md` — how to add features, update styles, extend the app",
    "3. `DESIGN_SKILL.md` — design reference before any CSS or UI work",
    "",
    "Claude Code loads `CLAUDE.md` automatically. Mention the other files explicitly when the task involves UI or feature work.",
    "",
    "---",
    "",
    "## Token Conservation Rules",
    "",
    "### 1. Give Claude a specific task, not a vague request",
    "",
    "**Wasteful:** \"Check the whole codebase and improve things.\"",
    "",
    "**Efficient:** \"In `task-app.js`, the `normalizeTask()` function needs a new `dueDate` field with a default of `null`. Add it and update the modal in `index.html` to include a date input.\"",
    "",
    "Specificity eliminates exploration rounds before any real work begins.",
    "",
    "### 2. Read SKILL files before starting feature work",
    "",
    "Before asking Claude to add a feature, make sure it has read the relevant skill file. This prevents Claude from inventing its own approach and then needing corrections.",
    "",
    "- Adding a keyboard shortcut → point Claude to `SKILL_ADD_SHORTCUT.md`",
    "- CSS or UI work → point Claude to `DESIGN_SKILL.md`",
    "- General feature → point Claude to `SKILL.md`",
    "",
    "### 3. Keep docs-content.js in sync — always",
    "",
    "`data/docs-content.js` is a pre-rendered cache of all markdown docs. When you change any `.md` file that appears in the docs index, you must also update the matching entry in `docs-content.js`.",
    "",
    "**Rule:** Any Claude session that edits a doc file must also update `data/docs-content.js` in the same commit.",
    "",
    "### 4. Commit after each phase of work",
    "",
    "Context windows are finite. Committing at natural boundaries gives you a clean rollback point and keeps the diff small for the next session.",
    "",
    "### 5. Use PHASES.md for context-window handoffs",
    "",
    "When a task is too large for one context window, `PHASES.md` has structured prompts for each build phase. Copy the relevant phase prompt into a fresh Claude Code window.",
    "",
    "---",
    "",
    "## The 5-Location Rule for Keyboard Shortcuts",
    "",
    "Every new keyboard shortcut touches exactly **5 locations**:",
    "",
    "| # | File | What to update |",
    "|---|---|---|",
    "| 1 | `task-app.js` | Key handler logic |",
    "| 2 | `styles.css` | New UI styles (if needed) |",
    "| 3 | `index.html` | Row in `#shortcutsPanel` |",
    "| 4 | `docs/KEYBOARD_SHORTCUTS.md` | Table row |",
    "| 5 | `data/docs-content.js` | Mirror the table row (cache sync) |",
    "",
    "See `SKILL_ADD_SHORTCUT.md` for the full step-by-step process with code templates.",
    "",
    "---",
    "",
    "## How to Add a New Doc",
    "",
    "1. Create the markdown file in `docs/`",
    "2. Add an entry to the `docs` array in `data/project-data.js`",
    "3. Add the cached content to `data/docs-content.js`",
    "",
    "The Docs tab in the browser will now show the new document automatically.",
    "",
    "---",
    "",
    "## How to Add a New Skill",
    "",
    "1. Create the skill file in the root of the project (e.g., `SKILL_SOMETHING.md`)",
    "2. Add an entry to the `skills` array in `data/project-data.js`",
    "3. Add the cached content to `data/docs-content.js`",
    "4. Reference the skill from `CLAUDE.md` so future sessions know when to read it.",
    "",
    "---",
    "",
    "## Giving Feedback to Improve AI Behaviour",
    "",
    "When Claude makes a mistake or misses a convention, update the relevant skill file so future sessions don't repeat it.",
    "",
    "- Wrong CSS pattern → add a note to `DESIGN_SKILL.md`",
    "- Wrong shortcut implementation → add a note to `SKILL_ADD_SHORTCUT.md`",
    "- General LBM development mistake → add a note to `SKILL.md`",
    "",
    "This turns one-off corrections into permanent guidance. The skill files are the institutional memory of this project.",
    "",
    "---",
    "",
    "## AI-Recommended Task Convention",
    "",
    "When an AI session recommends a new task, add it to `data/project-data.js` with these fields:",
    "",
    "```js",
    "{",
    "  id: \"LBM-XXX\",",
    "  title: \"What the task is\",",
    "  notes: \"Why it was recommended and what it involves.\",",
    "  lane: \"backlog\",",
    "  priority: \"P2\",",
    "  area: \"product\",",
    "  source: \"recommended\",",
    "  recommendedBy: \"Claude Sonnet 4.6\",",
    "  references: []",
    "}",
    "```",
    "",
    "---",
    "",
    "## Common Gotchas",
    "",
    "| Gotcha | What to do |",
    "|---|---|",
    "| `docs-content.js` shows old content in browser | Update the matching key in `data/docs-content.js` |",
    "| Task fields missing after seed reset | Add missing fields to `normalizeTask()` in `task-app.js` with safe defaults |",
    "| Board column not showing in list view | The lane key must be in `ACTIVE_LANES` in `task-app.js` |",
    "| Shortcut fires inside text inputs | Add the input guard at the top of the key handler (see `SKILL_ADD_SHORTCUT.md`) |",
    "| Style looks wrong after edit | Check `DESIGN_SKILL.md` Section 3 (Color System) and Section 4 (Spacing) |",
    ""
  ].join("\n"),

  "SKILL_ADD_SHORTCUT.md": [
    "# SKILL: Add a Keyboard Shortcut to LBM",
    "",
    "Use this skill whenever a new keyboard shortcut is added to the Local Business Manager. It ensures the shortcut appears consistently across all five locations it must exist.",
    "",
    "---",
    "",
    "## When to trigger",
    "",
    "The user says something like:",
    "- \"Add a shortcut for X\"",
    "- \"Make [key] do [action]\"",
    "- \"Add a keyboard shortcut to [action]\"",
    "",
    "---",
    "",
    "## The 5 locations — touch all of them",
    "",
    "Every new shortcut must be added to **all five** of these locations. Missing any one creates an inconsistency.",
    "",
    "| # | File | What to update |",
    "|---|---|---|",
    "| 1 | `task-app.js` | Add the key handler logic |",
    "| 2 | `styles.css` | Add any new styles needed |",
    "| 3 | `index.html` | Add a `<div class=\"shortcuts-row\">` in `#shortcutsPanel` |",
    "| 4 | `docs/KEYBOARD_SHORTCUTS.md` | Add a row to the right table |",
    "| 5 | `data/docs-content.js` | Mirror the change from step 4 (pre-rendered cache) |",
    "",
    "---",
    "",
    "## Step-by-step process",
    "",
    "### Step 1 — Implement in task-app.js",
    "",
    "**Guard pattern** — shortcuts must not fire when the user is typing:",
    "```javascript",
    "const tag = document.activeElement.tagName;",
    "if (tag === \"INPUT\" || tag === \"TEXTAREA\" || document.activeElement.isContentEditable) return;",
    "```",
    "",
    "**Context checks:**",
    "- Detail panel open: `!el.detailOverlay.hidden && detailTaskId`",
    "- Focused list row: `document.querySelector(\".list-row.is-focused\")`",
    "",
    "### Step 2 — Add styles if needed (styles.css)",
    "",
    "Only needed if the shortcut introduces new UI. Insert styles near the relevant section.",
    "",
    "### Step 3 — Add to shortcuts panel (index.html)",
    "",
    "Find `#shortcutsPanel` and add a row to the correct group:",
    "```html",
    "<div class=\"shortcuts-row\"><kbd>X</kbd><span>What it does</span></div>",
    "```",
    "",
    "### Step 4 — Update docs/KEYBOARD_SHORTCUTS.md",
    "",
    "Add a row to the correct table:",
    "```markdown",
    "| `X` | What it does |",
    "```",
    "",
    "### Step 5 — Update data/docs-content.js",
    "",
    "Find the `\"docs/KEYBOARD_SHORTCUTS.md\"` key and mirror the exact change from Step 4.",
    "",
    "---",
    "",
    "## Checklist before finishing",
    "",
    "- [ ] Key handler added in `task-app.js` with correct context guard",
    "- [ ] Styles added to `styles.css` (if new UI introduced)",
    "- [ ] `index.html` shortcuts panel has the new row in the right group",
    "- [ ] `docs/KEYBOARD_SHORTCUTS.md` table updated",
    "- [ ] `data/docs-content.js` cache updated to match the markdown",
    ""
  ].join("\n")
};
